name: Basic CI/CD Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest

    env:
      # Use your Docker Hub username here
      DOCKER_IMAGE: richsudo/myapp
      DOCKER_TAG: ${{ github.sha }}

    steps:
      # 1️⃣ Checkout code
      - name: Checkout code
        uses: actions/checkout@v3

      # 2️⃣ Run tests (optional)
      - name: Run unit tests
        run: |
          echo "Running tests..."
          # Replace this with your actual test command, e.g., npm test
          echo "Tests passed!"

      # 3️⃣ Log in to Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 4️⃣ Build Docker image
      - name: Build Docker image
        run: |
          docker build -t $DOCKER_IMAGE:$DOCKER_TAG .

      # 5️⃣ Push Docker image
      - name: Push Docker image
        run: |
          docker push $DOCKER_IMAGE:$DOCKER_TAG

      # 6️⃣ Deploy to staging (placeholder)
      - name: Deploy to Staging
        run: |
          echo "Deploy step placeholder."
          echo "In a real scenario, this would deploy $DOCKER_IMAGE:$DOCKER_TAG to a staging server."

